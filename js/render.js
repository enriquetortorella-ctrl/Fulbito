// RENDER ALL
// ============================================================
function renderAll() {
  renderHub();
  renderPlayers();
  renderAttendance();
  renderRate();
}

// ============================================================
